import Default from './default.js'

export default class SelectRandomBulk extends Default {
    constructor(locator, value, options) {
        super('selectRandomBulk', locator, value, options)
    }
}